// RobotBuilder Version: 0.0.2
//
// This file was generated by RobotBuilder. It contains sections of
// code that are automatically generated and assigned by robotbuilder.
// These sections will be updated in the future when you export to
// C++ from RobotBuilder. Do not put any code or make any change in
// the blocks indicating autogenerated code or it will be lost on an
// update. Deleting the comments indicating the section will prevent
// it from being updated in th future.
#include "SetShooterSpeed.h"
#define SHOOTER_SPEED_THRESHOLD      100.0
#define SHOOTER_SPEED_TIMEOUT          3.0
SetShooterSpeed::SetShooterSpeed(double speed) 
{
	// Use requires() here to declare subsystem dependencies
	// eg. requires(chassis);
	// BEGIN AUTOGENERATED CODE, SOURCE=ROBOTBUILDER ID=REQUIRES
	Requires(Robot::shooterWheel);
    // END AUTOGENERATED CODE, SOURCE=ROBOTBUILDER ID=REQUIRES
	
//   PIDcontroller = new PIDController(
//         SmartDashboard::GetNumber("Shooter Speed P Term"),
//         SmartDashboard::GetNumber("Shooter Speed I Term"), 
//         SmartDashboard::GetNumber("Shooter Speed D Term"),
//         Robot::shooterWheel->encoder, Robot::shooterWheel->motor);
   
   // remember the shooter speed
	shooterSpeed = speed;
	
	// setup a timer to ensure the command exits in a reasonable time
	shooterTimer = new Timer();
}
// Called just before this Command runs the first time
void SetShooterSpeed::Initialize() 
{
   // reset and start the timer
   shooterTimer->Reset();
   shooterTimer->Start();
   
   // setup the PID controller and enable
   PIDcontroller->SetSetpoint(shooterSpeed);
   PIDcontroller->SetAbsoluteTolerance(SHOOTER_SPEED_THRESHOLD);
   PIDcontroller->Enable();
}
// Called repeatedly when this Command is scheduled to run
void SetShooterSpeed::Execute() 
{
    // Testing with Joystick: 
//	 Robot::shooterWheelVoltage->motor->Set
//	    (Robot::oi->getDriveJoystick()->GetAxis(Joystick::kThrottleAxis));
}
// Make this return true when this Command no longer needs to run execute()
bool SetShooterSpeed::IsFinished() 
{
   return (PIDcontroller->OnTarget() || shooterTimer->Get() > SHOOTER_SPEED_TIMEOUT);
}
// Called once after isFinished returns true
void SetShooterSpeed::End() 
{
   // disable the PID controller and the timer
   PIDcontroller->Disable();
   shooterTimer->Stop();
}
// Called when another command which requires one or more of the same
// subsystems is scheduled to run
void SetShooterSpeed::Interrupted()
{
   End();
}
